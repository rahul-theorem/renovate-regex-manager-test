{
  "regexManagers": [
    {
      "fileMatch": ["config.yaml"],
      "matchStrings": ["\\s\\s-\\s(?<depName>.*?):(?<currentValue>.*?)@(?<currentDigest>sha256:[a-f0-9]+)\\n"],
      "datasourceTemplate": "docker"
    }
  ]
}
